name: authentication_repository
description: Dart package which manages the authentication domain.
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  equatable: ^2.0.3
  firebase_auth: ^3.2.0


dev_dependencies: 
  flutter_lints: ^1.0.0